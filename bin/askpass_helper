#!/usr/bin/osascript
# AppleScript askpass helper for sudo prompts

on run
    try
        return text returned of (display dialog "macOS setup: Administrator password required for app installation:" default answer "" with title "macOS Setup" with icon caution with hidden answer)
    on error
        return ""
    end try
end run